adding hierarchical relationships via REST API currupt data model	"In kurzen Schritten was ich gemacht habe:

1. 30 Geschäftsprozesse über die API erstellt (siehe 01_POST.log)
2. für 23 davon einen übergeordneten Prozess eingetragen (siehe 02_PUT.log)
3. Baumdarstellung der GP in iteraplan aufgerufen

Ergebnis: ""Ein allgemeiner technischer Fehler ist aufgetreten.""
Listendarstellung klappt hingegen.

Die entsprechenden Skripte für das erstellen/ändern der Prozesse kann ich euch gerne als .jar zur Verfügung stellen bei Bedarf. Manuell habe ich das Szenario noch nicht nachgestellt.

Meine aktuelle iteraplan Umgebung habe ich auch in dem Stand nach dem Fehler rumliegen."		Bug	iteraplan PO	08/May/15 11:27 AM	15/Jun/15 10:38 AM					api	hierarchy	rest							"19/May/15 1:54 PM;dne;Habe das Talend Skript angehängt. Folgende Vorbereitungen sind nötig damit es läuft:

1. docProcess.zip direkt unter Laufwerk D: entpacken (rootFolder für Talend ist D:\, kann bei Bedarf geändert werden).
2. im iteraplan Demo Daten Bundle die Attribute signavioId und signavioParent anlegen (Freitext, einzeilig), sowie die GP ""Landesbank Ebene 1"".
3. d_writeProcesses.zip entpacken (Ort sollte egal sein).
4. d_writeProcesses_run.bat ausführen, bzw .sh"	22/May/15 9:45 AM;dne;Habe eben den PC frisch hochgefahren, das Jenkins Bundle entpackt, die Lizenz eingetragen, Attribute und den Prozess angelegt und dann das Talend Skript laufen lassen. Ergebnis wie bisher... Neue Logs sind angehängt.	"22/May/15 11:48 AM;dne;Das Problem konnte ich jetzt eben auch bei Björn nachstellen. Was mir dabei noch aufgefallen ist: Das Wurzelelement kann man nicht mehr anzeigen lassen (Fehlerseite). In der DB direkt sah auf den ersten Blick aber alles in Ordnung aus ...
"	"09/Jun/15 9:57 AM;fam;Fix fits. Function now working without error.
Code is okey.
Ticket closed."																																																			
