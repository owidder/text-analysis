Add coloring options to LSD context menu	"Add options to context menu.

Change configuration according to user's choice.

Add items for enumeration properties
Add items for numeric properties

All options should be in one list sorted alphabetically

configuration class provides available properties for the currently selected type of content elements."		New Feature	iteraplan PO	04/Mar/15 10:36 AM	07/Apr/15 1:27 PM														"31/Mar/15 10:35 AM;tam;lsdContextMenuProcessor
	It's look like almost the same code repeats in the getColoringOptions
		items.push({
			text: ""Show ""+name+"" as color (form red to green)"",
			click: function() {
				configuration.setNumericColoringScheme(field.persistentName, ""redToGreen"");
				$scope.drawAll();
			},
			id: id+""-red-to-green""
		});
		
		=> An Idea: Probably it's possible to refactor to items.push(getItem(name, persistentName, profile));"	"02/Apr/15 2:14 PM;dasm;Verified
revision 878"	"07/Apr/15 7:36 AM;msr;Numeric coloring should be turned around, i.e.
for red to green: the minimum value should be green, the maximum value should be red (red to green) and
for dark to light: the minimum value should be light, the maximum value should be dark"	07/Apr/15 10:12 AM;alvo;done																																																			
