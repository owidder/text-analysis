Localise metamodel in NCD	Put translation keys in deeper layers and the translation itself in the templates	49715	Sub-task	iteraplan PO	04/Aug/15 2:33 PM	15/Sep/15 5:15 PM														21/Aug/15 3:40 PM;ach;contact [~msr] for details	"21/Aug/15 3:43 PM;msr;Open TODOs:

* NCD Legend: Color Legend

* NCD Legend: Path (use same logic as in NCD context menu)

* NCD Legend: Nesting

* A full functional test of the NCD at the end because the logic had been adjusted at several places



Already done:

* Context menu

* Title

* NCD legend: filtering for inner and outer elements"	10/Sep/15 4:02 PM;ach;-  ncd legend in internet explorer: not every key is translated (a note from [~fro])	"15/Sep/15 12:56 PM;tam;/trunk/app/js/directives/diagramLegendAttributes/directive.js

	What for was the renaming from 

	1. $scope.paths = [];

		to

		$scope.outerFilteringItemspaths = [];

	2. where $scope.outerFilteringItemspaths = []; will be used?



/trunk/app/js/common/utils.js

	Why angular.copy will be used?

	function translatePathNCD(pathObject, translate){

		// resolve parts of translation keys in the text field

		var translatedPathObject = angular.copy(pathObject);

		var path = pathObject.text.split(' - ');



		if ((path[1])) { // path array length == 2

			translatedPathObject.text = ""NCD.PATH"";

			translatedPathObject.translationData = {

				pathFrom: mmTranslateKey([path[0]]),

				pathTo:   mmTranslateKey([path[1]]) // not always defined

			};

			translatedPathObject.translationData = resolveTranslationDataInstant(translatedPathObject.translationData, translate);

		}

		else{ // path array length == 1

			translatedPathObject.text = mmTranslateKey([path[0]]);

			translatedPathObject.translationData = {};

		}

		return translatedPathObject;

	}	



/trunk/app/js/ncd/value.contextMenuOptions.js

	What are these TODOs?

	// TODO NCD TRANSLATE OK

	Do we need them?"																																																			
