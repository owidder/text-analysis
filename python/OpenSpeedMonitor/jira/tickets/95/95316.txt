Create DB-Entity for Saved Queries	"# Create DB table ""STORED_QUERIES"" for saved queries with the following columns:

## UUID (as hibernate id, see generation below, UNIQUE)

## METADATA (VARCHAR2(4000)) containing JSON with following fields

### NAME (name of saved query, UNIQUE per author)

### AUTHOR (userId of creator of the saved query)

### LAST_UPDATED (timestampInUTC)

## DESCRIPTION (description of saved query, VARCHAR2(4000))

## QUERY (saved query, VARCHAR2(4000))

# Create DAOs

# Create migration scripts



Generate the UUID with the following code:

{code}String uniqueID = UUID.randomUUID().toString();{code}"		New Feature	iteraplan Quickfox	06/Sep/16 10:38 AM	16/Sep/16 3:19 PM														13/Sep/16 2:56 PM;tsu;[~cst] checked the migration scripts																																																						
