View für die laufenden Batch-Jobs	"In dieser Admin-View sollen alle laufenden Batch-Jobs (z.B. DB-Cleanups) erkennbar sein.



Pro Batch-Aktivität:

* Typ der Aktivität (z.B. Job löschen)

* Konkret betroffene Domain (z.B. Job XY)

* Aktueller Status 

** aktiv / inaktiv / (abgeschlossen)

** Stage (Subaktivität, die gerade läuft, z.B. JobResults löschen)

** Fortschritt innerhalb der Stage (z.B. Zeitpunkt)

** Gesamt-Fortschritt

** Anzahl bisher aufgetretener Fehler

** Zuletzt aufgetretener Fehler als Text"		New Feature	OpenSpeedMonitor	16/Feb/15 10:35 AM	25/Mar/15 12:35 PM														"09/Mar/15 2:19 PM;bwo;Die View existiert nun.

Sie wird alle 5 Sekunden aktualisiert, um mögliche neue Daten anzuzeigen.

Optimieren:

- nur bei neuen Daten Anzeige aktualisieren

- nur die Zellen mit neuen Daten aktualisieren "	16/Mar/15 9:56 AM;nku;Erläuternden Text über die Tabelle.	"23/Mar/15 9:59 AM;nku;Noch zu tun:



* Löschen benötigen wir erst mal nicht.

* Bitte so sortieren, dass die neueste oben steht. 

* Tabelle paginieren.

* Enums Attribute geben: i18nCode. Hierüber dann die Werte mehrsprachig anzeigen.

* Ggf. View irgendwo in der Haupt-GUI einhängen.

* Ajax Call intelligenter machen: Nur noch für die aktiven Activities Updates holen.

* Tabelle sortierbar machen"	"24/Mar/15 2:34 PM;bwo;* Die Tabelle ist nun absteigend nach dem Datum sortiert.

* Tabelle ist paginiert

* Enums sind internationalisiert

* Ajax call erneuert nun nur noch die veralteten Spalten

* Tabelle ist sortierbar



Es fehlt weiterhin ein Einstiegspunkt über die Haupt-GUI"	25/Mar/15 12:34 PM;bwo;Es existiert nun ein Link im Administrations-Menü.																																																		
