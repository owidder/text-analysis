Tests schlagen nur auf der Konsole fehl	"Folgendes betrifft die Tests:

* de.iteratec.osm.csi.WeeklyShopIntTests

* de.iteratec.osm.csi.WeeklyShopMultipleCsiGroupsIntTests



Für man die Tests auf der Konsole aus mit ""test-app -integration"", so schlagen die Tests fehl.

Startet man die alle Integrationtests aus der IDE heraus, so sind diese grün.

Führt man in der IDE den Gradle Task ""integrationTest"" aus, so schlagen die beiden Tests auch fehl.

Führt man auf der Konsole ""test-app de.iteratec.osm.csi.* -integration"" aus, also testet nur das eine Package, so sind die Tests wieder grün."		Bug	OpenSpeedMonitor	02/Jun/16 9:44 AM	02/Jun/16 11:59 AM														02/Jun/16 9:48 AM;mmi;Wenn die Tests fehlschlagen, dann sind in beiden Tests csiAggregation.csByWptDocCompleteInPercent == null, was nicht sein soll	02/Jun/16 12:01 PM;mmi;Das Problem war die Veränderung der Metaklasse durch andere Tests und wurde durch die @ConfineMetaClassChanges([<Class>]) gelöst																																																					
