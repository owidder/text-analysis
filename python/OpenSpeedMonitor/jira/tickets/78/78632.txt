'contains' for Enum-Type Filter does not take user-input	"IS:

# open LSD

# click on an element > 'Content FIlter' > 'Add Filter'

# Add an enum-filter (e.g. _Complexity_)

# set operator to 'contains'

# enter some text, e.g. 'abc'

# click 'apply'

# the text-field contains 'true'



SHOULD:

the text-field should contain 'abc'





Probably related to: ITERAPLAN-4418"		Bug	iteraplan PO	29/Mar/16 2:13 PM	05/Apr/16 11:18 AM					filter	filtering	nic																																																													
