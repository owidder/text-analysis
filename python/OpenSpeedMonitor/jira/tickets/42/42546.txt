Erweiterung Logik Legendeneinträge um Internet-Anbindung	"Die Informationen in der Graphen-Legende sollen um die Information der verwendeten Anbindung erweitert werden.

* Messgröße (z.B. „fv doc complete“)

* System (z.B. „live“)

* Messschritt (z.B. „OTTO_Homepage_entry“)

* Location (z.B. „otto-prod-netlab:IE“)

* Anbindung (z.B. ""DSL 6.000"", ""UMTS"", ...)"		Task	OpenSpeedMonitor	03/Jun/15 5:38 PM	22/Jul/15 6:58 PM														"16/Jul/15 5:50 PM;nku;Bitte die noch fehlenden Tests in der folgenden Test-Klasse implementieren:

de.iteratec.osm.result.SummarizedChartLegendEntriesSpec



Ich habe diese Klasse heute geschrieben, da mir Tests für Deine Graph-Label-Summarization gefehlt haben.



In der unit-Test-Klasse EventResultDashboardServiceTests hattest Du zudem dafür gesorgt, dass die Tests grün sind, wenn ein Bug besteht. Nachdem ich die IT-677 gefixt hatte war der Test dann rot :(



Hierzu: 

Testdaten, die als Konstanten definiert sind (wie z.B. connectivity3Connectivity) sollten zur Erzeugung von Test-Daten verwendet werden, die dann in Assertions wieder zur Prüfung herangezogen werden. Du hast den Connectivity-Namen (1369821600000) zur Prüfung verwendet. Nicht aber bei der Erzeugung der Connectivity als Test-Datum. Zudem macht eine ""1369821600000"" im Legenden-Eintrag auch keinen Sinn. Vielleicht schauen wir da zusammen noch mal drauf.



Sorry für die Mäkelei..."																																																						
