index-Analyse	"Zur Optimierung der Indices auf der EventResult-Tabelle sollte eine Beispiel-Query mit unterschiedlichen Indices getestet werden.

Die Performance soll für die folgende Criteria-Abfrage optimiert werden
{code}
def criteria = EventResult.createCriteria()
		return criteria.list {
			eq('medianValue', true)
			between('jobResultDate', fromDate, toDate)
			'in'('cachedView', cachedViews)
			rlike('tag', rlikePattern)
		}
{code}"		Task	OpenSpeedMonitor	23/Sep/13 4:46 PM	31/Oct/13 3:01 PM																																																																				
