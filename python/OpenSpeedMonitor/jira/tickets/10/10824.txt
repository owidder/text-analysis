Assoziierte Domänen-Objekte werden in unit-Tests nicht mit persistiert	"In der Testklasse de.iteratec.ispc.LocationAndResultPersisterServiceTests scheitern unit-Tests, weil die JobResults zwar persistiert werden, nicht aber die assoziierten EventResults. 

Der Fehler liegt in der folgenden Methode:
{code}
protected saveResult(EventResult result, JobResult jobRun, MeasuredEvent step, CachedView view, Integer run,Boolean median,
			GPathResult viewTag){

		result.measuredEvent=step
		result.numberOfWptRun=run
// ...

//		result.save(flush: true, failOnError: true)
		jobRun.eventResults.add(result)
		jobRun.save(flush: true, failOnError: true)

		informDependents(result)
		
	}
{code}
Wenn ich die auskommentierte Zeile wieder hinzunehme ist's wieder grün. Laut Doku sollen aber assoziierte Domänen-Objekte genau so, kaskadierend persistiert werden. Im Produktivbetrieb lief der Code auch ...??"		Bug	OpenSpeedMonitor	16/Dec/13 5:45 PM	06/Jan/14 4:29 PM														"06/Jan/14 3:20 PM;drieger;Analoges Problem in EventResultServiceTests gefunden.

http://jira.grails.org/browse/GRAILS-10960"																																																						
