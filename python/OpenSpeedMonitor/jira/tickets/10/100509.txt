Anpassungen widescreen-Dashboard	"Das breite Layout mit den Cards gefällt mir nach wie vor gut auf dem Ergebnis-Dashboard. Die folgenden Dinge sind mir noch aufgefallen auf http://dev.openspeedmonitor.wpt.iteratec.de/eventResultDashboard/



Initialer Aufruf (ohne Diagramm):

* ""View a custom dashboard"" Auswahl sollte in einer Zeile sein mit den beiden Buttons ""Dashboard"" und ""Results"".

* Die oberen ""Laschen"" der Haupt-Registerkarten (nav-tabs) ""Timeframe & Filter jobs"" und ""Choose measured variables"" haben nur beim initialen Laden die richtigen Farben. Klickt man auf eines, so wechselt es von grau zu weiß, wenn man mit der Maus das Element verlässt. Es sollte grau bleiben.

* Die beiden Buttons ""Show"" und ""As CSV"" sollten oberhalb der gesamten well-cards sein. Am besten in line mit den Registerlaschen rechtsbündig, denke ich.

* Damit die ""Trim""-Controls in der ""Choose measured variables"" Redisterkarte vertikal nicht so viel Platz wegnehmen würde ich pro Messgrößen-Kategorie (Loadtimes, Count of Requests und Size of Requests) die inputs für above und below in einer Zeile unterbringen. Der Text ""below"" und ""above"" kann ja als placeholder drinstehen.

* Die angezeigten Informationen, was gerade ausgewählt ist sind so wie sie momentan sind eher störend. Eigentlich braucht man (wenn überhaupt) nur dass, was vom Default abweicht und gerade nicht sichtbar ist. Also z.B. wenn Trim-Werte eingetragen sind oder auf bestimmte Connectivities und/oder Browser eingeschränkt wurde. Angezeigt werden sollten diese Werte ganz oben rechtsbündig und damit rechts von den drei Haupt-Buttons ""Dashboard"", ""Results"" und ""View a custom dashboard"" (siehe oben).



Nach Klick auf Show (mit Diagramm):

* Wenn die Box der allen angezeigten Kurven gemeinsamen Eigenschaften leer ist sollte sie auch nicht angezeigt werden.

* Diese Box und auch die Legende würde ich über die ganze Breite des Screens darstellen (genau wie der Header des Akkordions ""Adjust chart"")

* Den Button ""Download chart as csv"" würde ich zusammen mit den beiden Buttons ""Show"" und ""As CSV"" inline oberhalb der well-cards anordnen (siehe oben).

* Die angezeigten Informationen, was gerade ausgewählt ist sind hier ungünstig und sollten in eingeschränktem Umfang oberhalb des Diagramms rechts angezeigt werden (siehe oben).

* Das gesamte ""Adjust Chart""-Akkordion würde ich in einen Dialog verlegen, der sich mittels eines Buttons öffnen lässt. Der Button könnte dann mit den drei bestehenden Buttons ""Show"", ""As CSV"" und ""Download chart as csv"" inline angeordnet sein. Die Änderungen, die man i Dialog vornimmt müssten allerdings idealerweise direkt im Diagramm sichtbar werden, wie das jetzt auch ist.

"		Improvement	OpenSpeedMonitor	25/Oct/16 10:51 PM	01/Nov/16 10:39 AM																																																																				
