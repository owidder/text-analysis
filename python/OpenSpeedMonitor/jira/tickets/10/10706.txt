Fehlerhaft MeasuredValue-tags im Altdatenbestand korrigieren	"Durch die Migration sind die tags der hourly-event-MeasuredValues nicht umgebaut worden. Diese tags müssten aus den folgenden 5 Bestandteilen bestehen:
* JobGroup-ID
* MeasuredEvent-ID
* Page-ID
* Browser-ID
* Location-ID

Statt dessen bestehen die tags noch immer aus den folgenden drei Bestandteilen:
* Job-ID
* Page-ID
* Browser-ID"		Bug	OpenSpeedMonitor	25/Oct/13 11:25 AM	25/Oct/13 7:00 PM														"25/Oct/13 2:04 PM;nkuhn;Lösung:

Alle hourly-event-MeasuredValues mit einem tag aus nur drei Bestandteilen (WHERE aggregator_id=1 and tag rlike '^[^;]*;[^;]*;[^;]*$') müssen korrigiert werden Dies geschieht mittels eines Java-Tools, welches sowohl auf die sqlite-, als auch auf die mysql-Datenbank zugreifen muss.

Die fünf Bestandteile des Ziel-tags müssten folgendermaßen ermittelt werden können:

* JobGroup-ID -> Hängt direkt am Job
* MeasuredEvent-ID -> Mittels Jobnamen den Job in der sqlite ermitteln. Dass daran hängende Script holen. Mit dem Namen des Scripts müsste ein MeasuredEvent existieren.
* Page-ID -> Aus dem alten tag
* Browser-ID -> Aus dem alten tag
* Location-ID -> Hängt direkt am Job

Kann einer der 5 nicht aufgelöst werden, bleibt der tag bestehen wie er ist."	"25/Oct/13 5:32 PM;nkuhn;Es bleiben zwei Tags mit 3 Bestandteilen übrig, die nicht bearbeitet werden können, weil die entsprechenden Jobs in der sqlite-DB mit identischem Namen doppelt vorkommen:
* 4;4;2
* 13;4;3
{code}
sqlite> select j.label, s.label from WPTJob j inner join WPTScript s on s.id=j.wptscriptid where j.label='FF7_BV1_Step04_ADS'; 
FF7_BV1_Step04_ADS|BV1_ADS
FF7_BV1_Step04_ADS|BV1_ADS_Poloshirt_Validierung
sqlite> select j.label, s.label from WPTJob j inner join WPTScript s on s.id=j.wptscriptid where j.label='IE8_BV1_Step04_ADS'; 
IE8_BV1_Step04_ADS|BV1_ADS_Validierung
IE8_BV1_Step04_ADS|BV1_ADS_Poloshirt_Validierung
{code}

Zudem bleiben 2 tags mit 3 Bestandteilen übrig, deren Jobs inzwischen gelöscht wurden (cleanupScript).
* 2;2;2
* 1;1;2"																																																					
