Reporting messender Agent -> Graphite	Es soll für jede Messung protokolliert werden, welcher Messagent diese Messung durchgeführt hat. Neuere WPT-Server (siehe OT-61) geben diese Info im REST-API-XML mit aus (<testerDNS>). Diese Info müsste als Attribut im JobResult gespeichert werden. Der Agent könnte auch eine eigene Domain werden, die n:m an der Location hängt.		Task	OpenSpeedMonitor	06/Dec/13 10:42 AM	23/Oct/15 11:24 AM														"21/Jan/14 4:44 PM;nkuhn;Leider steht in <testerDNS></testerDNS> nicht die kennzeichnende IP der Agenten drin, sondern die öffentliche oder sowas (sind für mehrere Agenten identisch und damit nicht brauchbar). Rechnername und IP-Adresse, sowie sie auf der Detail-Result-Seite des WPTServers angezeigt werden stehen im xml-Result gar nicht drin. In der Datei testinfo.json.gz im results-folder auf dem Server sind beide Informationen enthalten: 
z.B. ""tester"":""AGENT02-80.69.33.174"",""testerDNS"":""80.69.33.134""

tester wird auf der Detail-Seite angezeigt (common_lib.inc: Zeile 1005 ff in Version 2.13) und testerDNS ist im xml-Result enthalten. Man müsste tester zusätzlich oder statt testerDNS in die xml schreiben (xmlResult.php: Zeile 81 in Version 2.13). Dann könnte man beim Eintreffen neuer Results den Tester als domain-Objekt erzeigen, wenn es mit diesem tester-Namen für diese Location noch keinen gibt. Zudem könnte man die tester-ID als EventResult-Attribut speichern. Dann könnte man diese ID mit im Dashboard anzeigen und bekäme schneller einen Überblick, welche Ergebnisse von welchem Agenten stammen."	"31/Jan/14 3:31 PM;nkuhn;Der multistep-Server schreibt tester neben testerDNS bereits mit in die result-xml.
Am komfortabelsten wären separate Kurven für die einzelnen Agenten im Result-Dashboard.
Eine Kurve pro Location/Queue wäre aber weiterhin die Default-Darstellung."	23/Oct/15 11:24 AM;nku;Der Tester wird bereits persistiert und im Hover der Datenpunkte angezeigt.																																																				
