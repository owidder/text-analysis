Alte Jobs löschen, BEVOR die Daten migriert werden.	"Es gibt eine Reihe von Jobs, die nicht mehr benötigt werden.
Im beigefügten SQLite-Script sind diese Jobs per SQLite-ID zu erkennen.

In der MySQL des iteraSpeedMonitors solllen nun die 
- WptJobConfig
- WptJobRun
und
- WptResult

dieser Jobs gelöscht werden. ACHTUNG: Die SQLite-IDs entsprechen NICHT den MySQL IDs, daher muss die Abbildung per Job-Label erfolgen.

Erwarteter Vorteil: Schnellere Migration und weniger bei der Migration erstellte MeasuredEvent."		Improvement	OpenSpeedMonitor	01/Oct/13 9:45 AM	16/Oct/13 4:56 PM														"08/Oct/13 4:32 PM;nkuhn;sqlite-Skript anpassen, sodass es weniger delete-Statements werden.
Mysql-Skript erzeugen, welches alle Jobs nach label löscht. Zugehörige JobResults und EventResults müssen mit gelöscht werden."	"16/Oct/13 10:59 AM;mzeimer;Das CleanUp-Tool soll eine zusätzliches SQL-script für die MySQL des ISM schreiben.

In diesem Zuge soll das vorhandene SQL-script für die SQLite optimiert werden: Die Results sollen nicht einzeln gelöscht werden, sondern über ein Delete über die Rückreferenz ""wptjobid""."	16/Oct/13 2:29 PM;mzeimer;Version 1.1.0.201310161425: Clean-up für aktuelles DB-Format, Alt-Format folgt.	"16/Oct/13 4:54 PM;mzeimer;In Version 1.4.0.201310161633 fertig.

Aufruf:

  java -jar cleanup-script-genearator.jar 20130916_wpt_monitor.sqlite server-dir-clean-up.bash sqLite-clean-up.sql ism-mysql-clean-up-script.sql ism-legacy-mysql-clean-up-script.sql 463 464 129 157

Dieser Aufruf würde für die SQLite (WPT Monitor) Job-IDs 463, 464, 129 und 157 4 Skripte erzeugen:

server-dir-clean-up.bash -> bash-script mit rm-Statements zum löschen der Verzeichnisse auf dem WPT server.

sqLite-clean-up.sql -> zum Aufräumen der SQLite selbst (es empfiehlt sich, dies am Ende zu tun)

ism-mysql-clean-up-script.sql -> zum Aufräumen der ISM DB ab Version 2.1.6

ism-legacy-mysql-clean-up-script.sql -> zum Aufräumen der ISM DB in Version 1.0.16 (und NUR dieser Version!)"																																																			
