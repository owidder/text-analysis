Status should be an option for selecting the linetype	It is not possible to define the line type of an element in LSD (of InformationSystems) by the property "status". This should be possible.		Improvement	iteraplan Thunderduck	23/Jan/17 11:07 AM	27/Jan/17 9:59 AM														"27/Jan/17 9:00 AM;sfe;The type ""TechnicalComponent"" can, just as ""InformationSystem"", use status for the line type. Please include that as well.

Side note: Instead of doing this

{noformat}

        property = type.properties.filter(function (attribute) {

            return attribute.persistentName === actionData.propertyPersistentName;

        })[0];

{noformat}

you can use this:

{noformat}

        property = type.propertyBy(actionData.propertyPersistentName);

{noformat}"																																																						
