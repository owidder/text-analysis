Aufbau der WPT-Monitor-Jobverwaltung im iteraspeedMonitor	"Entstanden aus ersten Diskussionen mit dem Kernziel der Gesamtablösung des WPT-Monitors.

* Die Zeitsteuerung soll mittels einer cron-Notation möglich sein.
* Es müssen Variablen definiert werden können.
* Nicht mehr benötigte Artefakte des Proxy-Mechanismus sollen alle in das iteraSpeedProxy-Plugin verlegt werden (wenn sie nicht schon hier liegen).
* Alle Artefakte aus dem iteraSpeedProxy-Plugin, die nicht zum Proxy gehören müssen in den iteraSpeedMonitor verlegt werden"		Story	OpenSpeedMonitor	15/Jul/13 12:57 PM	14/Mar/14 3:28 PM														"20/Jan/14 4:01 PM;drieger;* Description: Textarea
* Cron-String verständlicher Titel
* Layout Checkboxen Zugehörigkeit zu Labels
* Server-Vorauswahl für Locations
* Filtercombobox für Skripte: Möglichkeit zu filtern und alle Skripte anzuzeigen

Erledigt: Implementierung in Commit 41db5b1 in iteraSpeedOttoCsi"	"20/Jan/14 4:18 PM;drieger;* Mit Quartz Scheduler vertraut machen und feststellen, ob man dessen Jobs dynamisch zur Laufzeit einstellen, ändern und löschen kann.
* Mit der REST-API des WptServers vertraut machen und anschließend (mit dem HttpRequestService) Requests absetzen, um Jobs einzustellen."	"23/Jan/14 4:15 PM;drieger;JobProcessingService: Es lassen sich nun interaktiv über die GUI Jobs starten und die Ergebnisse betrachten. Auch die Implementierung für Job-Ausführungen ohne Nutzerinteraktion ist abgeschlossen. Diese startet auf dem ausgewählten WPT-Server einen Testlauf und fragt diesen so lange an, bis der Test abgeschlossen ist. Anschließend verwendet sie den LocationAndResultPersisterService um die Ergebnisse in der Datenbank zu persistieren.

Jobs lassen sich nun mit weiteren Parametern konfigurieren, u.a. mit den Advanced-Paramatern der WPT-Oberfläche und mit benutzerdefinierten Konnektivitätsoptionen."	24/Jan/14 3:25 PM;drieger;Die Quartz-Integration steht grundlegend. Pro Job lässt sich ein Cron-String eintragen. Dieser wird beim Speichern auf Korrektheit überprüft. Jobs lassen sich an Quartz übergeben und wieder stoppen, dies geschieht jedoch noch nicht automatisch beim Setzen/Entfernen des "Aktiv"-Status eines Jobs.	27/Jan/14 5:47 PM;drieger;Quartz-Jobs werden nun automatisch durch Setzen/Entfernen von "Aktiv" verwaltet. Aktive Jobs werden beim erstmaligen Start des ISM automatisch mitgestartet. Die Ausführung eines Jobs per Cron-String inkl. Eintragen der Ergebnisse in die Datenbank läuft nun.																																																		
