Action column bugs	"BUGS:

- _it is possible_ to drag an another column behind (after) the action column. 

- _it is possible_ to drag the action column since some other column is placed behind it

- _it is possible to remove all the data columns (except action column). it depends on the constant of minimum possible columns shown currently (increase to 2 - one for the last column, one for the actions column)



*it shouldn't*"		Bug	iteraplan Thunderduck	18/Jan/17 9:56 AM	19/Jan/17 5:20 PM																																																																				
