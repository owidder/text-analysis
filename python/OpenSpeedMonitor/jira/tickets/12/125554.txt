Job "Speichern unter" defekt	Wenn man einen Job editiert und dann auf _Speichern als_ klickt kommt ja das Javascript-Popup zum Eingeben eines neun Namens. Wenn ich hier einen neuen Namen eingebe lande ich wieder in der edit-View mit einer Fehlermeldung "Der Job konnte nicht gespeichert werden, weil es den Namen schon gibt" oder so ähnlich und im Jobname-Feld steht auch wieder der Name des ursprünglichen Jobs. Ich muss dann hier nochmals den neuen Namen eingeben und auf den `create`-Button klicken, dann erst wird der Job gespeichert.  		Bug	OpenSpeedMonitor	27/Jun/17 5:07 PM	29/Jun/17 9:54 AM																																																																				
