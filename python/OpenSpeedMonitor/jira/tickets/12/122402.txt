Enum attributes are not editable in BB list	"Steps to reproduce:

1. Add enum column (e.g. Accountability)

2. Column is editable (cell values can be edited)

3. Refresh the page



Actual result: enum column is not editable, strange propertyType appears in URL for Accountability (\{\{de.iteratec.iteraplan.model.attribute.ResponsibilityAT.Accountability}})

Expected result: enum column is still editable, URL is correct"		Bug	iteraplan PO	25/May/17 2:38 PM	11/Jun/17 1:49 PM																																																																				
