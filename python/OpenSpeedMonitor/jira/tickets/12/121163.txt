CI-Skript für ad-hoc-Tests erzeugen können	"Für einen Mess-Job soll im OSM eine Funktion implementiert werden, mit der man sich ein Skript (groovy oder bash) generieren lassen kann, welches zur Ausführung der ad-hoc-Messung aus der CI-Pipe verwendet werden kann.



Dieses Skript soll in etwa folgendes leisten:

 # Mess-Job OSM über neue REST-Funktion triggern.

 # Mit der zurückbekommenen Test-ID eine Zeit X lang prüfen, ob der Test fertig ist.

 # Anhand des erhaltenen Testergebnisses pro Seitentyp eine Bewertung bereitstellen.



Punkt 3 wäre idealerweise bereits in der Response des OSM-REST-Calls enthalten. Dann hätten wir diese Thresholds/Performance-Budgets schon im OSM und können diese später auch hier verwenden (z.B. für Alerting oder zur Visualisierung)."	121161	Sub-task	OpenSpeedMonitor	11/May/17 5:32 PM																																																																					
