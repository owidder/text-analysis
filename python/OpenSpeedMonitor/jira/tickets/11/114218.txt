Health-Status Service + Seite implementieren	"Ein Service bzw eine Seite die den Status vom OSM anzeigt.

Service deshalb, weil diese Infos später ggf. auch auf der Startseite eingeblendet werden sollten, wenn etwas Rot ist.

Ideen für Health-Status:

- Wenn der DetailAnalysis-Service aktiviert ist, ist er auch erreichbar und der API Key korrekt?

- Gibt es mindestens einen konfigurierten WebPagetest-Server der auch erreichbar ist und Agenten zugewiesen hat?

- Gibt es eine valide CSI Konfiguration?"		Improvement	OpenSpeedMonitor	08/Mar/17 9:26 AM																																																																					
