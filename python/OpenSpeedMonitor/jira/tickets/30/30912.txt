Nicht-Median-Ergebnisse nicht mehr standardmäßig persistieren	"Daten bei den es sich nicht um Mittelwerte handelt sollen nicht mehr standardmäßig mit persistiert werden. (Die Datenbank wird bei nicht Verwendung mit nicht benötigten Daten befüllt)



* Option in Job-Einstellungen (Attributen)

** persistNonMedianResults (default: false)



*Muss im {{LocationAndResultPersisterService}} berücksichtigt werden*"		Task	OpenSpeedMonitor	30/Jan/15 10:39 AM	16/Mar/15 10:37 AM														"04/Mar/15 1:42 PM;rhc;* Attribut in Job Domain-Klasse hinzugefügt {{persistNonMedianResults}}

** default Wert auf {{true}} gesetzt, um den Zustand der Anwendung mit hinzufügen der Modifikation nicht zu verändern



* Job-Sicht mit Checkbox versehen, die den Zustand des {{persistNonMedianResults}} wiedergibt. An dieser Stelle kann der Zustand geändert werden



* {{LocationAndResultPersisterService}} angepasst - in Methode {{persistResultsOfOneTeststep}}

** EventResults direkt vor Weitergabe an die Persistierungsmethode abgefangen und geprüft

*** *Codestellen kommen doppelt vor, daher evtl. optimieren*"	"16/Mar/15 10:36 AM;rhc;Job angepasst:

* Domain-Klasse (enthält Attribut persistNonMedianResults und ist standardmäßig auf true, somit bei Aktivierung des Features keine direkte Auswirkung zu vorher)

* LocationAndResultPersisterService (auf neue Funktion angepasst)

* Unit-Test erstellt

** de.iteratec.osm.measurement.environment.wptserverproxy.PersistingNewEventResultsWithNoMedianOptionTestSpec

** deckt genau den Fall ab ob im Job das Attribut persistNonMedianResults gesetzt ist oder nicht"																																																					
