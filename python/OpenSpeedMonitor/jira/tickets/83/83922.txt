Threadfix-Export refaktorieren	"Der Threadfix-Export ist momentan nur als Durchstich implementiert und sollte deshalb refaktoriert werden. Das betrifft u. a.:

* Das Parsen der Zap-Ergebnisse aus ES (nach Möglichkeit nur einen Parser verwenden)

* Kibana- und Threadfix-Export in eigene Klassen auslagern

* PersistenceProvider sollten sich nur um das Schreiben in ES oder Threadfix kümmern und keine Veränderung an den Daten vornehmen

* Das Erstellen der JSON und XML Inhalte sollte ebenfalls in eigenen Klassen gekapselt sein

* Die Discovery über Consul muss überarbeitet werden, weil Consul für Threadfix Http als Protokoll angibt, obwohl es Https sein müsste"		Story	secureCodeBox	11/May/16 11:34 AM		secureBoxEngine				iteratec																																																															
