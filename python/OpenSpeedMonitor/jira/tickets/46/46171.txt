REST test EE/Win7/FF24.0/Mysql	Executing the REST test case for above stated system configuration	46148	Sub-task	iteraplan PO	21/Oct/13 11:57 AM	23/Oct/13 5:23 PM														"21/Oct/13 4:25 PM;svo;- ein Header mit dem Namen ""Format"" existiert meines wissens nach nicht in Http. Der Header der für die Informationen gedacht ist, die wir hier abfragen wollen, heißt ""Accept"". Wenn man die entsprechenden Testfälle gegen diesen Header ausführt, funktionieren sie.

- Eine Anfrage auf einen einzelnen Building Block, der nicht vorhanden ist, liefert eine leere Ergebnismenge zurück, die Test-Spezifikation fordert allerdings einen 404.

- Für die REST-API sind lediglich funktionale Berechtigungen vorgesehen (sprich, entweder die komplette Schnittstelle nutzen oder gar nicht), die Test-Spezifikation fordert jedoch 401 falls ein Nutzer ohne Berechtigungen export/import versucht oder einen BB anfragen will, dessen Typ er nicht lesen darf. Hier ist vermutlich die Dokumentation der REST-Permission upzudaten, dass die entsprechenden Rechte beinhaltet sind. "	"23/Oct/13 9:19 AM;sfe;I don't quite understand the remarks on what was wrong in TC4 steps 5, 6 and 7 and the ""actual result"" is missing, too.
Export/Import tests (TC8) seem to have been skipped, probably because the test spec was regarded as wrong here (according to comment by Sebastian Vöst above: REST permission overruling missing Import permission). May have been good nonetheless to verify the actual behavior.

As far as I know, building block type read permissions should still affect the REST api as well, and indeed do so according to my own tests. Thus TC8 step 11 should have been executed, in my opinion, but no result is given.

Aside from that, everything seems fine. Putting the issue back on ""ready to test"" for someone else to look over it, if they want to, or until Sebastian is here again to clarify the points above."	23/Oct/13 5:23 PM;kgo;Seems fine.	"24/Oct/13 3:41 PM;svo;updated test documentation according to more detailed specification.

Remaining Differences between Test Spec and behaviour:
- sending a GET to a QueryResource with an invalid query or a query for a BBT for which the user has no read permissions results in a 400 - no such universal type error.
- importing/exporting excel without functional permissions for import/export works. this is intended behaviour, the test specification should be adjusted here."	"25/Oct/13 9:47 AM;psc;
Regarding mentioned ""Remaining Differences"":

1) sending a GET to a QueryResource with an invalid query or a query for a BBT for which the user has no read permissions results in a 400 - no such universal type error.

--> the respective issues have already been reported and are tracked in ITERAPLAN-2072.

2) importing/exporting excel without functional permissions for import/export works. this is intended behaviour, the test specification should be adjusted here.

--> correct. see analysis in ITERAPLAN-2070 and decision in ITERAPLAN-2046."	25/Oct/13 9:48 AM;psc;Since all identified issues are tracked in dedicated ticket, this test-ticket will be closed.																																																	
