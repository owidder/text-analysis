REST API chokes on some data constellations	"Depending on the data and specific relations of building blocks, the current REST impl seems to choke.
Observation by customer:
{quote}
Aber folgende URIs funktionieren

-          data.json?content=InformationSystem[@name=""ITB""];

-          data.json?content=InformationSystem[@name=""ITB""]

-          data.json?content=InformationSystem[@name=ITB];

-          data.json?content=InformationSystem[@name=ITB]

-          data.json?content=InformationSystem%5B%40name%3D""ITB""%5D%3B                            (= URL encodiert)

 

Und folgende funktionieren leider nicht, der Unterschied ist mir nicht klar:

-          data.json?content= InformationSystem[@name=""PDB""];

-          data.json?content=InformationSystem%5B%40name%3D""PDB""%5D%3B
          data.json (Ohne Param) =>  An internal server error has occurred.

(Alle hier angegebenen ITERAQLs liefern nat??rlich die erwartete Liste ??ber das GUI)
{quote}

Debug information or details for the specifics of problematic constellations are not yet available. The customer's log file contained no relevant information."		Bug	iteraplan PO	23/May/13 6:26 PM	29/Aug/13 12:33 PM					REST									09/Aug/13 1:25 PM;bra;do we have database dumps to reproduce this one? using bankdata i was unable to reproduce any of the abovementioned issues.	"19/Aug/13 10:27 AM;lba;*Could not Reproduce:*
data.json (Ohne Param) => An internal server error has occurred.
provoke error with a simple query (IS [@name=...]

*Errors:*
Hashtags in requests lead to internal server error:
InformationSystem[@name .contains('#')];
url encoded hashtags works fine:
InformationSystem[@name .contains('%23')];

Ampersand in requests lead to internal server error:
InformationSystem[@name .contains('a') & @name .contains('b')];
url encoded ampersand works fine:
InformationSystem[@name .contains('a') %26 @name .contains('b')];

*Also:*
spaces in the url don't affect the request

Tested with FF 22 on Windows 7 and using bankdata
"	29/Aug/13 11:38 AM;svo;This issue will be addressed in ITERAPLAN-1881	29/Aug/13 12:33 PM;svo;In 1881, the entire API will be revamped. This should not be an issue anymore afterwards.	29/Aug/13 12:33 PM;svo;Will be fixed with 1881 anyway.																																																		
