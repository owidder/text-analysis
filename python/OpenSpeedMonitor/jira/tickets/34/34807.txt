Unit-Test OsmChartPoint fixen	"Die Domain ist mit @EqualsAndHashcode annotiert.

Seitdem schlägt ihr Unit-Test fehl."		Task	OpenSpeedMonitor	16/Mar/15 10:13 AM	16/Mar/15 2:57 PM														"16/Mar/15 11:38 AM;rhc;* Die Klasse ist mit Java-Code geschrieben und sollte um innerhalb der Anwendung Konsistent zu bleiben in Groovy-Code umgeschrieben werden.

* -Attribut-Bedinnungen setzten und mit validate() prüfen (Annotation @Validateable)- Verworfen, funktioniert nicht so wie es soll in Nicht-Domain-Klassen

* -Die Klasse muss als Domain-Klasse implementiert werden (momentan noch in Sources:groovy/report/chart)- soll nicht in Datenbank persistiert werden !"	"16/Mar/15 12:43 PM;rhc;-Um Methoden von Validateable in Unit-Test zu nutzen, muss die non Domain-Class gemockt werden:-

-*MockUtils.mockCommandObject(OsmChartPoint.class)*-

-Danach steht die validate()-Methode im Unit-Test zur Verfügung-

Verworfen, siehe vorheriger Kommentar"	"16/Mar/15 2:55 PM;rhc;* Klasse in Groovy-Code umgeschrieben.

* EqualAndHasCode Annotation hinzugefügt.

* isValidate Methode implementiert, da es sich hier um keine Domain-Klasse handelt (somit validate() und hasErrors() nicht möglich) und diese Klasse nur temporär genutzt werden soll.

* Alle Klasse die vorher über den Konstruktor das Objekt erzeugt haben angepasst.

* Komplette Test-Suite durchlaufen.

** Keine Fehler im Zusammenhang mit den Änderungen aufgetreten"																																																				
