Beim Löschen älterer Daten eine BatchActivity erzeugen	"* Für jeden per Quartz-Job gestartete Löschvorgang eine BatchActivity erzeugen.

* Progress und Status müssen aktualisiert werden.

 * Die Activity muss abbrechbar sein.

* Wenn der Lösch-Job noch läuft (Activity als noch aktiv) soll kein neuer Job starten. "		New Feature	OpenSpeedMonitor	16/Mar/15 9:45 AM	31/Mar/15 11:00 AM														"17/Mar/15 11:07 AM;rhc;Jeder Lösch-Job erzeugt bei Ausführung eine eigene BatchActivity

* JobResult mit abhängigen Objekten (HttpArchive und EventResult)

* MeasuredValue und MeasuredValueUpdateEvent



*Noch zu behebendes Problem:*

* Mit neustart der Anwendung ist der Quartz-Job nicht mehr vorhanden.

* Die BatchActivity zum Quartz-Job jedoch in der Datenbank

** Beim Hochfahren der Anwendung muss geprüft werden ob aktive BatchActivities vorhanden sind

** Handelt es sich dabei um Quartz-Jobs, müssen diese gestartet werden. 



Vielleicht ähnliche Lösung wie bei {{Measurements Generally Enabled}} im OsmConfigurationController?"	"23/Mar/15 8:58 AM;rhc;* DbCleanupServiceSpec angepasst

** BatchActivityService in ServiceMocker implementiert (für Unit-Tests)  "	"23/Mar/15 10:15 AM;nku;BatchActivities des nächtlichen Löschens sollen beim Starten der Anwendung auf cancelled gesetzt werden.

Trotzdem soll der Quartz-Job nur zum Löschen und der Erzeugung einer Activity führen, wenn der Schalter ""measurements generally enabled"" gesetzt ist.



Siehe auch IT-525"	"31/Mar/15 10:59 AM;rhc;Vollzogene Änderungen:



* Wenn während des nächtlichen Löschen die Anwendung Herunterfährt, werden die nicht beendeten BatchActivities beim starten der Anwendung auf den Status CANCELLED gesetzt.

* Nächtliche Quartz-Jobs starten nur, wenn sich {{MeasurementsGenerallyEnabled}} auf {{true}} befindet, siehe dazu auch IT-525

* Spock-Test für BatchActivityService wurde erzeugt: {{BatchActivityServiceSpec}}"																																																			
