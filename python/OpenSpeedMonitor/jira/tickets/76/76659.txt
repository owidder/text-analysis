outer/outer nesting	"* max 3 level outer/outer nesting

* entry in the according tool area to select the self relationship for nesting

* Consult [~wfr] or [~sfe] before starting this issue



# tool area -> configuration

# item builder creates hierarchical items (different from inner nesting, the original items are moved when added as someone's child, not copied)

# color shading in item decorator after base color is determined (is done in layouter already)

# layouter modification"		New Feature	iteraplan PO	10/Mar/16 1:35 PM	26/Apr/16 1:14 PM																																																																				
