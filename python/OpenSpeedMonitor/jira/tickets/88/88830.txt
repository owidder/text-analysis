REST-API Methode zum persistieren von Assets	"Wir brauchen im DetailDatenService die folgenden REST-Methoden für die Entgegennahme der Aufträge zum Laden von Assets:



*/api/perssitAssetsForWptResult POST*

Zu übergebende Daten pro nachzuladendem Result:

* osmId

URL der OSM-Instanz

* baseUrlWptServer

* TestId

Test-ID des WPT-Servers

* jobGroupId



Die folgenden Daten liest der DetailDatenService aus dem WptServer-XML:

* measuredEventId (median.<firstView|repeatView>.eventName)

Die page-Id leitet der service aus seinem eigenen Mapping ab.

* locationId (location.split(':')[0])

* browserId (location.split(':')[1])

* bwDown

* bwUp

* latency

* plr

Lesen tut er Strings. Die zugehörigen IDs kommen aus dem Mapping.



Um mehrere Results auf einmal zu persistieren  könnte 

* eine Liste von testIds übergeben werden. Dann müssten alle andereen Parameter identisch sein.

* eine Liste von DTO's übergeben werden. Jedes DTO enthält dann alle erforderlichen Daten."		Task	OpenSpeedMonitor	29/Jun/16 1:07 PM	07/Jul/16 1:10 PM														06/Jul/16 8:05 AM;bwo;Es existiert nun ein RestApiController, über welche die Persistierung getriggert werden kann. Dieser ruft bereits die entsprechenden Methoden für die Persistierung auf, diese sind jedoch nicht implementiert, hierfür muss zunächst IT-1191 fertig gestellt werden.	06/Jul/16 8:06 AM;bwo;Geblockt durch IT-1191																																																					
