Formatter erstellen	"Dieser könnte dann für eine initiale formatierung des gesamten Codes verwendet werden.

Zudem kann dieser mit unter Versionskontrolle."		Task	OpenSpeedMonitor	25/Jan/16 2:11 PM	03/Feb/16 11:56 AM														"03/Feb/16 11:56 AM;bka;Nach einigen Einstellungen am Code Style, lässt sich die AutoFormat-Funktion beinahe verwenden.



Es ist mir allerdings nicht gelungen die Einstellung zu finden, die folgendes Problem behebt:

AutoFormat versucht Zeilen die zu lang sind, umzubrechen. Dabei wird bspw. aus folgender Zeile

{code}

~/^${jobGroupIDPattern};${measuredEventIDPattern};${pageIDPattern};${browserIDPattern};${locationIDPattern}$/

{code}



diese Zeile:

{code}

~/^${jobGroupIDPattern};${measuredEventIDPattern};${pageIDPattern};${browserIDPattern};${

    locationIDPattern

}$/

{code}



In dieser Form kann der Compiler allerdings den Code nicht mehr verstehen. Daher ist die AutoFormat-Funktion für uns (erstmal) nicht zu gebrauchen."																																																						
