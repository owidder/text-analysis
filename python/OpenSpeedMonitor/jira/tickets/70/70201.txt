Abruf der Job-Status auf der Jobliste erfolgt hart per HTTP	"Wird der OSM per HTTPS aufgerufen so wird der HTTP-Request mit einer Fehlermeldung quittiert und nicht abgesetzt:



{code}

Mixed Content: The page at 'https://iteraspeedmonitor.otto.wpt.iteratec.de/job/list' was loaded over HTTPS, but requested an insecure XMLHttpRequest endpoint 'http://iteraspeedmonitor.otto.wpt.iteratec.de//job/getRunningAndRecentlyFinishedJobs'. This request has been blocked; the content must be served over HTTPS.

{code}



Getestet mit Firefox und Chrome."		Bug	OpenSpeedMonitor	21/Jan/16 9:06 AM	13/Feb/16 6:52 PM														"25/Jan/16 4:23 PM;bka;Dinge, die hinter einem Reverse-Proxy getestet werden müssen:

- ohne gesetzte grails.serverUrl

- mit gesetzter grails.serverUrl, und absoluten Links

- mit gesetzter grails.serverUrl, und relativen Links"	"01/Feb/16 4:15 PM;nku;In Test-Deployment testen, ob das Setzen von grails.serverURL auf http://hdhdhdh statt https://bfsdfhjs  zu diesem Fehler führt.

"	13/Feb/16 6:52 PM;nku;Lag nur an einer falschen serverURL in der externen config Datei.																																																				
