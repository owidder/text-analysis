Iteraplan wird automatisiert auf Schwachstellen getestet	"Iteraplan wird automatisiert durch unser SecurityDevOps Tool auf schwachstellen getestet. 



Abnahmekriterien:

- Es existert ein funktionierender Jenkins Job auf dem Iteratec Jenkins server welcher einen Scan anstoßen kann. (nicht automatisiert jede x Tage)

- Es sollen alle relevanten Funktionen der Web Applikation gespidert und gescannt werden. 

- Es entsteht kein zusätzlicher (Spider- und Scan-) Aufwand durch unnötige Tests."		Story	secureCodeBox	14/Nov/14 12:10 PM	28/Nov/14 9:57 AM														"28/Nov/14 9:57 AM;bwo;IteraPlan läuft nun auf dem Projekt Server.

Der Job startet IteraPlan mit jedem run selbstsän dig und beendet ihn anschließend."																																																						
